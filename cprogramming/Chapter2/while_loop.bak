#include<stdio.h>
#include<conio.h>

void main() {
	int n, i = 0, temp;
   long max;
	/*
   printf("Input only number: "); scanf("%c", &n);
   if (n >= 48 && n <= 57) {
   	while(i <= n ) {
      	printf("Hello motherfucker &c", n);
         printf("\n");
         i++;
      }
   } else {
   	printf("Wrong input ! please try again");
   }
   */
	printf("\tFind maximum of number");
	printf("\nInput number: "); scanf("%d", &n);
   temp = n + '0';
   if (temp >= 48 && temp <= 57) {
   	while(i <= n) {
      	printf("Value: %d", i);
      	max = n;
         i++;
         printf("\n");
      }
      printf("Maximum number: %d	", max);
   } else {
   	printf("Wrong input ! please try again");
   }
   getch();
}