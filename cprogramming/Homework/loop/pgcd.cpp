#include<stdio.h>
#include<conio.h>

void main() { /* PGCD */
	int first_number, second_number, temp pgcd;
   printf("\t\tProgram Plus Grand Common Diviser\n");
   printf("First number: "); scanf("%d", &first_number);
   while (first_number > 0) {

   }
   if (first_number <= 0 && second_number <= 0) printf("Invalid input, Please try again");
   else printf("PGCD: %d", pgcd);
   getch();
}